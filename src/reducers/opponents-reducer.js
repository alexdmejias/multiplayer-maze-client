import * as types from '../types';

const initialState = {
  opponents: {}
};

export default function player (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
